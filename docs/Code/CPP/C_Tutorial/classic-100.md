# C 经典100例

本页整理适合练习 C 基础语法、控制流、数组、指针、字符串、结构体和文件操作的经典题型。建议每题都独立编写、编译并测试边界输入。

## 入门题

1. 输出 Hello World。
2. 输入两个整数并输出和。
3. 摄氏温度和华氏温度转换。
4. 判断奇偶数。
5. 判断闰年。
6. 求三个数最大值。
7. 交换两个变量。
8. 计算圆面积。
9. 求阶乘。
10. 打印乘法表。

## 控制流与函数

11. 判断素数。
12. 输出指定范围内的素数。
13. 求最大公约数。
14. 求最小公倍数。
15. 斐波那契数列。
16. 递归求阶乘。
17. 递归求字符串长度。
18. 判断回文数。
19. 判断水仙花数。
20. 简单计算器。

## 数组与排序

21. 数组求和。
22. 数组最大值和最小值。
23. 数组逆序。
24. 冒泡排序。
25. 选择排序。
26. 插入排序。
27. 二分查找。
28. 合并两个有序数组。
29. 去除数组重复元素。
30. 统计数组元素频率。

## 字符串

31. 统计字符个数。
32. 字符串复制。
33. 字符串连接。
34. 字符串比较。
35. 字符串反转。
36. 判断回文字符串。
37. 统计单词数量。
38. 查找子串。
39. 字符大小写转换。
40. 删除字符串中的空格。

## 指针与内存

41. 指针交换两个变量。
42. 指针遍历数组。
43. 动态分配数组。
44. 动态二维数组。
45. 函数返回状态并通过指针输出结果。
46. 函数指针调用。
47. 使用 `qsort` 排序整数。
48. 使用 `qsort` 排序结构体。
49. 实现简单动态字符串。
50. 检查内存分配失败。

## 结构体与文件

51. 学生结构体输入输出。
52. 结构体数组排序。
53. 链表创建。
54. 链表插入。
55. 链表删除。
56. 链表查找。
57. 文本文件复制。
58. 统计文件行数。
59. 读取 CSV 简单字段。
60. 二进制文件读写结构体。

## 综合练习

61. 通讯录管理。
62. 学生成绩管理。
63. 简单词频统计。
64. 简单日志过滤器。
65. 简单十六进制查看器。
66. Base64 编码理解练习。
67. 简单 XOR 编码器。
68. 命令行参数解析。
69. 简单配置文件解析。
70. 简单栈实现。

## 进阶练习

71. 简单队列实现。
72. 循环队列实现。
73. 哈希表实现。
74. 二叉树遍历。
75. 表达式括号匹配。
76. 简单计算表达式求值。
77. 迷宫 DFS。
78. 迷宫 BFS。
79. 文件加密解密练习。
80. 简单压缩统计。

## 安全与边界

81. 安全读取一行输入。
82. 检查整数溢出。
83. 安全拼接字符串。
84. 避免数组越界。
85. 避免格式化字符串漏洞。
86. 检查 `malloc` 返回值。
87. 防止 use-after-free。
88. 防止 double free。
89. 使用 AddressSanitizer 检查程序。
90. 使用 UndefinedBehaviorSanitizer 检查程序。

## 项目题

91. 简单 Todo 程序。
92. 简单文本数据库。
93. 简单 HTTP 请求解析器。
94. 简单 ELF 魔数检测器。
95. 简单 PNG 文件头检测器。
96. 简单端口配置解析器。
97. 简单单元测试框架。
98. 简单 Makefile 多文件项目。
99. 简单命令行工具。
100. 综合复习项目。
