# SQL CREATE DATABASE

`CREATE DATABASE` 用于创建一个新的数据库。

## 示例

```sql
CREATE DATABASE shop_db;
```

## 注意点

- 是否允许当前用户创建数据库，取决于权限设置。
- 字符集、排序规则、所有者等参数因数据库不同而不同。

## 相关链接

- 上一篇：[SQL INSERT INTO SELECT](insert-into-select.md)
- 下一篇：[SQL CREATE TABLE](create-table.md)
