# SQL LEN()

`LEN()` 用于返回字符串长度，在 SQL Server 中很常见。

## 示例

```sql
SELECT LEN(name)
FROM users;
```

## 方言差异

- SQL Server 常用 `LEN()`
- MySQL 常用 `LENGTH()` 或 `CHAR_LENGTH()`

## 相关链接

- 上一篇：[SQL MID()](mid.md)
- 下一篇：[SQL ROUND()](round.md)
