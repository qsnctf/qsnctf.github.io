# SQL NOW()

`NOW()` 常用于返回当前日期时间。

## 示例

```sql
SELECT NOW();
```

## 方言差异

- MySQL 常用 `NOW()`
- SQL 标准常见 `CURRENT_TIMESTAMP`
- SQL Server 常见 `GETDATE()`

## 相关链接

- 上一篇：[SQL ROUND()](round.md)
- 下一篇：[SQL FORMAT()](format.md)
