# HTTP2

HTTP/2 是 HTTP/1.1 之后的重要版本，目标是在保持 HTTP 语义的同时提升传输效率。

## 主要变化

- 二进制分帧。
- 多路复用，同一连接上并发多个请求响应。
- 头部压缩 HPACK。
- 流优先级。
- 服务端推送曾被支持，但现代浏览器中支持情况已经变化。

## 与 HTTP/1.1 的关系

HTTP/2 保留方法、状态码、Header 等语义，但底层传输格式不同。抓包时看到的是帧，而不是纯文本请求行。

## 安全关注

HTTP/2 引入新的实现复杂度。分析时应关注代理转换、请求走私、头部规范化差异和服务端对帧状态的处理。
