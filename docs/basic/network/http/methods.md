# HTTP 请求方法

HTTP 方法表示客户端希望对资源执行的动作。

| 方法 | 常见用途 |
| --- | --- |
| GET | 获取资源 |
| POST | 提交表单或数据 |
| PUT | 上传或替换资源 |
| PATCH | 部分修改资源 |
| DELETE | 删除资源 |
| HEAD | 只获取响应头 |
| OPTIONS | 查询支持能力 |
| TRACE | 回显请求，通常禁用 |

## 安全注意

方法名不是权限控制。服务端必须在认证和授权后判断用户是否可以执行对应操作。仅在前端隐藏按钮不能阻止直接构造请求。
